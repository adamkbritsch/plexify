Flask==3.0.3
spotipy==2.24.0
APScheduler==3.10.4
SQLAlchemy==2.0.32
gunicorn==22.0.0
python-dotenv==1.0.1
requests==2.32.3
rapidfuzz==3.9.7
plexapi==4.15.16
PyYAML==6.0.2
mutagen==1.47.0
# SpotiFLAC v0.7.1+ imports httpx + tqdm but does NOT declare them in its
# package metadata, and the in-app auto-update runs `pip install --no-deps`.
# Pin them here so they're always present (this outage: picker_tick crashed
# every tick with ModuleNotFoundError: No module named 'httpx', 2026-05-30).
httpx==0.28.1
tqdm==4.67.3
# PINNED, not @main. Tracking main is how this broke: the API was renamed under us
# (1.1 `run()` -> 1.3 `run_async()`), and 1.7 added a provider that needs a browser.
# Upgrade deliberately, test a real download, then bump this. 1.7.3 verified working
# (Qobuz, ~10s/track) on 2026-08-13; 1.1.2 could no longer fetch from any mirror.
SpotiFLAC==1.7.3
# SpotiFLAC imports pydoll at package import time (via its Amazon provider) but does not
# declare it, so without this the whole library fails to import — same undeclared-dep
# trap as httpx/tqdm above. Plexify never drives the browser; the import just needs it.
pydoll-python==2.9.0
# Telegram source: drive @BeatSpotBot (FLAC) as a USER session (bots can't DM bots).
Telethon==1.36.0
# Smart update: Claude (Opus) self-heals broken SpotiFLAC git-main updates. Only called
# when an update breaks the import AND the free deterministic patch can't fix it.
anthropic>=0.92.0
inotify_simple==1.3.5
